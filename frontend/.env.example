# Dev mode (Vite proxy handles /auth, /engine-rest etc. automatically)
# Leave VITE_API_URL empty — the proxy in vite.config.ts takes care of it
VITE_API_URL=

# OR if you want to bypass the proxy and call the backend directly:
# VITE_API_URL=http://localhost:8080
